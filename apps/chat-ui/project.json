{
  "name": "chat-ui",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "prefix": "app",
  "sourceRoot": "apps/chat-ui/src",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@analogjs/platform:vite",
      "options": {
        "configFile": "apps/chat-ui/vite.config.ts",
        "main": "apps/chat-ui/src/main.ts",
        "outputPath": "dist/apps/chat-ui/client",
        "tsConfig": "apps/chat-ui/tsconfig.app.json"
      },
      "defaultConfiguration": "production",
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "sourcemap": false,
          "mode": "production"
        }
      },
      "outputs": ["{workspaceRoot}/dist/apps/chat-ui"]
    },
    "serve": {
      "executor": "@analogjs/platform:vite-dev-server",
      "continuous": true,
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "chat-ui:build",
        "port": 4200
      },
      "configurations": {
        "development": {
          "buildTarget": "chat-ui:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "chat-ui:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "serve-static": {
      "continuous": true,
      "executor": "@nx/web:file-server",
      "options": {
        "buildTarget": "chat-ui:build",
        "port": 4200,
        "staticFilePath": "dist/apps/chat-ui/browser",
        "spa": true
      }
    },
    "test": {
      "executor": "@analogjs/vitest-angular:test",
      "options": {
        "config": "vite.config.ts"
      }
    }
  }
}
